<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Style="{StaticResource DefaultPage}"
             x:Class="ShareYourFood.SignUp">
    <ContentPage.Content>
        <StackLayout Padding="48,48,48,48" VerticalOptions="Center">
            <Entry Placeholder="Imię" Style="{StaticResource Entry}" />
            <Entry Placeholder="Nazwisko" Style="{StaticResource Entry}" />
            <Entry Placeholder="Hasło" IsPassword="True" Style="{StaticResource Entry}" />
            <Entry Placeholder="Powtórz hasło" IsPassword="True" Style="{StaticResource Entry}" />
            <Entry Placeholder="E-mail" Style="{StaticResource Entry}" />
            <Button Text="Zarejestruj się" Margin="0,32,0,0" Style="{StaticResource Button}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>