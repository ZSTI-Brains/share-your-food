<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Style="{StaticResource DefaultPage}"
             x:Class="ShareYourFood.MainPage">

    <StackLayout Padding="48" VerticalOptions="Center">
        <Image Source="logotype.png" HeightRequest="128" Margin="0,0,0,48" />
        <Button Text="Logowanie" Clicked="SignIn" Style="{StaticResource Button}" />
        <Button Text="Rejestracja" Clicked="SignUp" Style="{StaticResource Button}" />
        <Button Text="Kontynuuj bez logowania" 
                Clicked="OpenMap"
                Margin="0,32,0,0"
                FontFamily="{StaticResource DefaultRegular}"
                FontSize="18"
                BackgroundColor="Transparent" 
                TextColor="#242424" />
    </StackLayout>
</ContentPage>
